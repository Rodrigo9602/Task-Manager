import { StateColorDirective } from './state-color.directive';

describe('StateColorDirective', () => {
  it('should create an instance', () => {
    const directive = new StateColorDirective();
    expect(directive).toBeTruthy();
  });
});
